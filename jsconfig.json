{
    "compilerOptions": {
        "baseUrl": "src", // Set base URL to src
        "paths": {
            "@/*": ["*"] // Map @ to src
        },
        "jsx": "react" // Ensure this is set if using React
        // ... other options ...
    }
}
